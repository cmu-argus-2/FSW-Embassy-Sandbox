[package]
name = "fsw-test"
version = "0.1.0"
edition = "2024"


[dependencies]
embedded-hal-mock = { version = "0.11", features = ["eh1", "embedded-hal-async"] }
tokio = { version = "1", features = ["macros", "rt"] }
# tells cargo to look at workspace root for embassy-lib
fsw-lib = {workspace = true}
embedded-hal-async = "*"
embedded-sdmmc = "0.9.0"
